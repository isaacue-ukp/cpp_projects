#include <bits/stdc++.h>
using namespace std;

// 常用类型缩写
using ll = long long;
using pii = pair<int, int>;
using vi = vector<int>;
using vll = vector<ll>;

// 常用常量
constexpr int INF = 1e9 + 7;
constexpr ll LINF = 1e18 + 7;
constexpr int MOD = 1e9 + 7;

void solve() {
    // 你的核心算法逻辑写在这里
}

int main() {
    // 关闭同步，加速 cin/cout 输入输出
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    // cin >> t; // 如果存在多组测试数据，取消此行注释
    while (t--) {
        solve();
    }

    return 0;
}